1) self.__bot.message_handler(commands=["start"])(self.start)
=> Декоратор. Используется для обработки введённого текста

2) self.__bot.callback_query_handler(
            func=lambda c: c.data.startswith("interest_") or c.data == "interest_done"
        )(self.handle_interest_inline_callback)
=> Декоратор. используется для обработки call-back'a от кнопки

3) self.__bot.send_message(message.chat.id, "Введите свой возраст:")
=> Отправляет сообщение пользователю в чат

4) self.__bot.register_next_step_handler(
                message, self.user_registration, step="age"
            )
=> Декоратор. Указывает каким методом обработать следующее введённое сообщение

5) self.__bot.answer_callback_query(call.id, "Вы выбрали: Мужской")
=> Вывод уведомления о нажатие, например, кнопки

6) markup.add(
            InlineKeyboardButton(text_m, callback_data="user_m_sex"),
            InlineKeyboardButton(text_f, callback_data="user_f_sex"),
        )
=> установка текста call-back для обработки
Пример обработки:
def handle_user_sex_inline_callback(self, call):
        user_id = call.message.chat.id
        user = self.__users.get(user_id)
        if not user:
            return

        updated = False
        if call.data == "user_m_sex":
            if user.sex != "М":
                user.sex = "М"
                updated = True
            self.__bot.answer_callback_query(call.id, "Вы выбрали: Мужской")



####################
бд консоль
####################
Вход
sqlite3 my_database.db

Посмотреть список таблиц
.tables

Просмотреть структуру таблицы
.schema users

Сделать выборку данных
SELECT * FROM users;

Удалление таблицы
DROP TABLE users;


Выйти из SQLite
.quit
